{
  "name": "mireva-studio",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local-first collaborative interface design studio with a native WebGPU compositor",
  "license": "MIT",
  "engines": {
    "node": ">=22.16"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs"
  }
}
